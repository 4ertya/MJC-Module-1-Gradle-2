
version 'unspecified'

repositories {
    flatDir {
        dirs 'libs' // инициализируем папку, хранящую aar-архивы как репозиторий
    }
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'

    compile files('libs/utils-1.3.5.jar')
}
